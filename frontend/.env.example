# Set this to the backend URL in production (e.g., https://your-backend-app.onrender.com)
# Default is http://localhost:8000 for local development (which Vite proxies by default anyway)
VITE_API_BASE_URL=http://localhost:8000
VITE_DEBUG=false
